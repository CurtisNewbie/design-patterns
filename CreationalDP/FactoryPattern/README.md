# Factory Design Pattern

Creating an object through a commonly used class, which instantiates objects of interfaces without exposing the details of the actual implementation. The implementation of these objects can vary, but the users will not need to know.

The user of the "factories" will not be coupled with the actual implementation of the interfaces, rather, they only use the methods defined on the interfaces.
